import React, {FC} from 'react';

import css from './footer.module.css'

const Footer:FC = () => {
    return (
        <div className={css.footer}>
            Footer
        </div>
    );
};

export {Footer};