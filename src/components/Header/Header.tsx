import React, {FC} from 'react';


import css from './header.module.css'

const Header: FC = () => {
    return (
        <div className={css.head}>
            Header
        </div>
    );
};

export {Header};